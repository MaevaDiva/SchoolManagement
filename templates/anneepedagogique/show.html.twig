{% extends 'base.html.twig' %}

{% block title %}Anneepedagogique{% endblock %}

{% block body %}
    <h1>Anneepedagogique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anneepedagogique.id }}</td>
            </tr>
            <tr>
                <th>LibelleAnnee</th>
                <td>{{ anneepedagogique.libelleAnnee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_anneepedagogique_index') }}">back to list</a>

    <a href="{{ path('app_anneepedagogique_edit', {'id': anneepedagogique.id}) }}">edit</a>

    {{ include('anneepedagogique/_delete_form.html.twig') }}
{% endblock %}
