{% extends 'base.html.twig' %}

{% block title %}Edit Anneepedagogique{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header">    <h1>Modifier Anneepedagogique</h1>  </div>
        <div class="card-body">
            {{ include('anneepedagogique/_form.html.twig', {'button_label': 'Update'}) }}

        </div>
        <p>    <a href="{{ path('app_anneepedagogique_index') }}" class="btn btn-outline-secondary">back to list</a>    </p>
    </div>

   <p>{{ include('anneepedagogique/_delete_form.html.twig') }}</p>
    
{% endblock %}
